[project]
name = "email-triage-env"
version = "0.1.0"
description = "Email classification environment"
dependencies = [
    "flask",
    "openenv>=0.2.0",
    "openai==1.30.1"
]

[project.scripts]
server = "server.app:main"
